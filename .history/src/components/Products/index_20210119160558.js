import React from "react";

const Products = () => {
  return (
    <ProductsContainer>
      <ProductsHeading>Heading</ProductsHeading>
          <ProductWrapper>{data.map[(product, index) => {
              return {
                  < ProductCard key = { index } >
                      
                  </>
              }
      }]}</ProductWrapper>
    </ProductsContainer>
  );
};

export default Products;
